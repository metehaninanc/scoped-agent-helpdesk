{
  "name": "@helpdesk/agent",
  "version": "0.0.0",
  "private": true,
  "description": "Identity agent: Agent SDK wrapper, one file, zero built-in tools. Must not import gateway runtime code, only its type definitions; its own audit writes go through the shared @helpdesk/audit-core package instead.",
  "type": "module",
  "scripts": {
    "test": "pnpm --filter @helpdesk/audit-core build && vitest run",
    "test:watch": "pnpm --filter @helpdesk/audit-core build && vitest",
    "typecheck": "pnpm --filter @helpdesk/gateway build && tsc -p tsconfig.json --noEmit",
    "build": "pnpm --filter @helpdesk/gateway build && tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.273",
    "@helpdesk/audit-core": "workspace:*",
    "@helpdesk/gateway": "workspace:*"
  }
}
